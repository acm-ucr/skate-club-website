import React from "react";
import { Calendar } from "../ui/calendar";

const SkateCalendar = () => {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <Calendar className="h-screen" />
    </div>
  );
};

export default SkateCalendar;
